<Window x:Class="IpScopePro.Views.FloatingAlertsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:IpScopePro.Converters"
        Title="{converters:Translate Key=AlertsWindowTitle}" Height="Auto" Width="340"
        WindowStyle="None" AllowsTransparency="True"
        Topmost="True" ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        Background="Transparent" FontFamily="Segoe UI"
        Top="{Binding WindowTop}" Left="{Binding WindowLeft}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:DateTimeFormatConverter x:Key="DateTimeFmt" />
    </Window.Resources>

    <Border Background="{DynamicResource WindowBg}" CornerRadius="12"
            BorderBrush="{DynamicResource BorderColor}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" BlurRadius="16" Opacity="0.4" />
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource TitleBarBg}" CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="{converters:Translate Key=AlertsHeader}" FontSize="11" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentColor}" VerticalAlignment="Center" Margin="12,0" />

                    <Button Grid.Column="1" Content="─" ToolTip="{converters:Translate Key=Minimize}" Width="28" Height="26"
                            Style="{DynamicResource FlatButton}"
                            Foreground="{DynamicResource SubtleFg}" Command="{Binding ToggleMinimizeCommand}"
                            FontSize="13" />

                    <Button Grid.Column="2" Content="✕" ToolTip="{converters:Translate Key=Hide}" Width="28" Height="26"
                            Style="{DynamicResource FlatButton}"
                            Foreground="{DynamicResource RedColor}" Click="Close_Click"
                            FontSize="12" />
                </Grid>
            </Border>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,7" BorderBrush="{DynamicResource SeparatorColor}" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Orientation="Horizontal">
                                    <TextBlock Text="{Binding NewStatusText}" FontSize="12"
                                               FontWeight="Bold" Foreground="{DynamicResource RedColor}" />
                                    <TextBlock Text="  " />
                                    <TextBlock Text="{Binding Hostname}" FontSize="12"
                                               Foreground="{DynamicResource WindowFg}" FontFamily="Consolas"
                                               FontWeight="SemiBold" />
                                </StackPanel>

                                <TextBlock Grid.Row="1" Text="{Binding Timestamp, Converter={StaticResource DateTimeFmt}, ConverterParameter='HH:mm:ss'}"
                                           FontSize="9" Foreground="{DynamicResource MutedFg}" Margin="0,3,0,0" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</Window>
